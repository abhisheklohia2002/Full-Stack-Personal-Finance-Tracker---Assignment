// 'admin', 'user', 'read-only'
export enum UserRole {
  ADMIN = "admin",
  CUSTOMER = "user",
  VIEW = "read-only",
}

export enum category {
  Food = "Food",
  Transport = "Transport",
  Entertainment = "Entertainment",
}
